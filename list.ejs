<!DOCTYPE html>
<html lang="kr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>문서</title>
</head>
<script src="https://code.jquery.com/jquery-3.4.1.min.js"></script>
<body class="index_html">

  <div class="nav_bar">
    <a href="/menu">
      <li class="bar_content">메뉴</li>
    </a>
    <a href="/write">
      <li class="bar_content">작성하기</li>
    </a>
  </div>

  <h1 class="top_list">글 목록</h1>

  <div class="mid_list">

    <% posts.reverse() %>

    <% for (var i=0; i < posts.length;  i ++) { %>

    <div class="lists">
      <li>
        <span class="orange"><%= posts[i].post_mbti %>,
        <span class="black"><%= posts[i].user_name %></span>
      </li>
      <li class="borders">
        <span class="gray"><%= posts[i].showing_date %>에 등록</span>
      </li>

      <br>
      <textarea class="aligns addr" readonly><%= posts[i].post_description %></textarea>
      
      <br>

      <button class="ud" data-id="<%= posts[i]._id %>">DELETE</button>

    </div>

      <br>

      <% } %>

      <script>
        var txtArea = $(".addr");
        if (txtArea) {
          txtArea.each(function () {
            $(this).height(this.scrollHeight);
          });
        }
      </script>

      <script>

        $('.ud').click(function(event_) {
          var pwisit = prompt('글 작성시 이용한 비밀번호를 입력해주세요.')
          var 글번호 = event_.target.dataset.id; 
          var 지금누른거 = $(this);

          $.ajax({
            method: 'DELETE',
            url: '/editordelete',
            data: {
              _id: 글번호,
              pwisit: pwisit
            },
          })
          .done((result) => {
            $(event_.target).parent('div').fadeOut();
          })
          .fail(function(xhr, textStatus, errorThrown) {
            alert('비밀번호를 잘못 입력하셨습니다.')
            console.log(xhr, textStatus, errorThrown)
          })
        });

      </script>

  </div>

</body>

<style>

  @import url(https://cdn.jsdelivr.net/gh/moonspam/NanumSquare@1.0/nanumsquare.css);

  .black {
    color: black;
  }

  .orange {
    font-weight: 600;
    color: rgb(4, 71, 255);
    text-align: left;
  }

  .aligns {
    background-color: rgb(250, 250, 250);
    text-align: left;
    font-family: 'NanumSquare', sans-serif;
    font-size: 17.5px;
    width: 500px;
    padding: none;
    border: none;
    overflow: hidden;
    line-height: 30px;
    resize: none;
  }

  textarea:focus {
    outline: none;
  }

  body {
    margin: 0px;
    font-size: 18px;
    font-family: 'NanumSquare', sans-serif;
    margin-bottom: 100px;
  }

  li {
    list-style: none;
  }

  a {
    text-decoration: none;
  }

  .borders {
    padding-bottom: 24px;
    border-bottom: 1px solid gray
  }

  .ud {
    transition-duration: 0.2s;
    width: 100px;
    height: 40px;
    margin: 5px;
    border: none;
    background-color: rgb(4, 71, 255);
    color: white;
    border: 1px solid rgb(4, 71, 255);
    border-radius: 10px;
  }

  .ud:hover {
    transition-duration: 0.2s;
    background-color:  rgba(4, 71, 255, 0.8);
    border: 1px solid rgba(4, 71, 255, 0.8);
  }

  .lists {
    border: 1px solid gray;
    border-radius: 20px;
    padding: 20px;
    margin-bottom: 30px;
    width: 500px;
    line-height: 35px;
    display: inline-block;
    background-color: rgb(250, 250, 250);
    text-align: center;
    box-shadow: 2px 2px 2px rgb(200, 200, 200);
  }

  .gray {
    color: rgb(200, 200, 200);
  }
  
  .mid_list {
    margin-top: 50px;
  }
  
  .top_list {
    margin-top: 50px;
        margin-top: 100px;
  }

  .index_html {
    text-align: center;
  }

  .nav_bar {
    width: 100%;
    background-color: rgb(220, 220, 220);
    text-align: center;
    border-bottom: 2px solid rgb(4, 71, 255);
    position: fixed;
    top: 0px;
  }

  .bar_content {
    transition-duration: 0.4s;
    display: inline-block;
    width: 100px;
    padding: 12px;
    margin: 5px;
    text-align: center;
    color: rgb(100, 100, 100);
    border-radius: 10px;
  }

  .bar_content:hover {
    background-color: rgb(180, 180, 180);
  }

</style>

</html>